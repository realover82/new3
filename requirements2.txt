# 이미지 처리 및 데이터 분석
numpy
opencv-python==4.9.0.80  # 특정 버전이 명시되어 있습니다.
Pillow
pandas
numpy
# 이미지 증강 및 전처리
imgaug
tqdm

# 딥러닝/머신러닝 (PyTorch 관련)
# 이 라이브러리들은 일반적으로 pip install 명령으로 설치합니다.
# PyTorch는 CUDA 버전에 따라 설치 명령이 다를 수 있으므로,
# 아래 torch/torchvision/torchaudio는 기본 CPU 버전으로 작성했습니다.
# (GPU 사용 시 PyTorch 공식 가이드에 따라 설치 명령을 변경해야 합니다.)
torch
torchvision
torchaudio

# 병렬 처리 및 기타 유틸리티
# glob, os, math, csv, re, multiprocessing, concurrent.futures는 Python 표준 라이브러리이므로 명시적으로 설치할 필요는 없지만,
# 명확성을 위해 포함할 수도 있습니다. 여기서는 주요 외부 패키지만 포함했습니다.